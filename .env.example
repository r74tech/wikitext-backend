# Node Environment
NODE_ENV=development

# Server Configuration
PORT=3000
APP_PORT=3000
# APP_HOST=127.0.0.1  # Uncomment to bind to all interfaces (0.0.0.0)

# Database Configuration
POSTGRES_USER=wikitext
POSTGRES_PASSWORD=changeme
POSTGRES_DB=wikitext
POSTGRES_PORT=5432
# POSTGRES_HOST=127.0.0.1  # Uncomment to bind to all interfaces (0.0.0.0)

# DATABASE_URL=postgres://wikitext:changeme@localhost:5432/wikitext

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:4173,https://wd.r74.tech

# Logging
LOG_LEVEL=info

# pgAdmin Configuration (optional)
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=changeme
PGADMIN_PORT=5050
# PGADMIN_HOST=127.0.0.1  # Uncomment to bind to all interfaces (0.0.0.0)

# Future: Wikidot Integration
# WIKIDOT_API_KEY=
# WIKIDOT_SITE_ID=